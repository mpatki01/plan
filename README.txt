
To run the import functionality:
1. cd into import-geonames
2. issue npm install (npm must be installed).
3. run import-all.sh from a *NIX bash prompt.

To run the web application:
1. cd into web
2. issue npm install (npm must be installed).
3. issue node app.js (node must be installed).
